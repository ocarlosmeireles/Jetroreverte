import React from 'react';

// This component is not used and is intentionally left blank as its functionality was
// tied to Firebase, which has been removed from the project.
const ResetPassword = (): React.ReactElement | null => {
    return null;
};

export default ResetPassword;
