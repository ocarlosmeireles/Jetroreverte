import CollectionWorkspace from './CollectionWorkspace';

// FIX: This file was empty, causing module resolution errors.
// It seems to be an old reference to CollectionWorkspace, so we're re-exporting it
// to fix imports in other files without changing them.
export default CollectionWorkspace;
