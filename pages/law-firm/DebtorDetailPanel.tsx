import React from 'react';

// This component is obsolete. Its content and logic have been moved into
// the new popup modal inside DebtorsRegistry.tsx to fulfill the user's request
// for an "iOS style" popup instead of a side panel.
const DebtorDetailPanel = () => {
    return null;
};

export default DebtorDetailPanel;