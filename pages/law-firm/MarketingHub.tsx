import React from 'react';

// Component deprecated as per user request to remove Marketing Hub.
const MarketingHub = (): React.ReactElement | null => {
    return null;
};

export default MarketingHub;